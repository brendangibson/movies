module.exports = {
  purge: [],
  theme: {
    boxShadow: {
      // Unused for now - was intended to provide vignette for box art but sizes are inconsistent
      boxart: "inset 0 2px 4px 0 rgba(0, 0, 0, 0.5)",
    },
    minHeight: {
      40: "10rem",
    },
  },
  variants: {},
  plugins: [],
};
